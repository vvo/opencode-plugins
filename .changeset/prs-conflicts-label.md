---
"opencode-prs": patch
---

Show `conflicts` in red as the status of a PR with merge conflicts, keep `×` for failing checks only, and show `approved · pending` instead of `approved · blocked` while required checks run.
